{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt && python -m reflex init && python -m reflex export --frontend-only --no-zip"
  },
  "deploy": {
    "startCommand": "python -m reflex run --env prod --backend-only --backend-host 0.0.0.0 --backend-port $PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}